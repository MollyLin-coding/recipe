# 南坡萬酒廠 APP 開發接手文件 v10.15（v2.5：建單空酒款紅字警示 + 刪除訂單 admin）
---

## 第三十七章：v2.5（建單空酒款紅字警示 + 刪除訂單 admin）（2026-07-21）

> 正式 tag **v2.5**（回滾 `rollback.sh v2.4`）。回報：`回報_建單空酒款警示與刪除訂單_20260721.md`。

### 37.0 背景（BUG-調查結案）
主公/Molly 回報「建單按儲存並關閉無反應」＝**建單防呆**（`doCreate`：`if(!items.length)` 沒加任何酒款就擋下）＋**舊提示藏太爛**（底部小 toast、2.2s、離按鈕遠、DevTools 開著被面板擋）。查證過程已排除程式/後端/檔案/擴充功能（詳見 master `_20260721` BUG 章）。**結論：非 bug，是 UX**。本章即修 UX。

### 37.1 建單空酒款 → 明顯紅字警示（純前端）
- 建單 modal 標題列下方加 `#om_warn` 紅框（預設 `display:none`）：「⚠️ 請先至少選擇一款酒（酒款＋數量），才能儲存並關閉」。
- `doCreate` 的 `!items.length` 分支：顯示 `#om_warn` + `orderItemLines.scrollIntoView` 捲到酒款區 + 保留原 toast。
- 酒款列 `oi_x_rec` 加 `oninput` → 一輸入酒款紅框自動隱藏。

### 37.2 刪除訂單（後端 + 前端，**admin 限定**）
- **後端** `deleteOrder(p)`：`if(p._role!=='admin') return 僅管理員可刪除`；找到 orderNo 列 → `ws.deleteRow(i+1)` → `_logOrderChange_(…,'刪除訂單',…)`。doGet 加 case。**admin 閘用 session `p._role`**（token→_getSession_→p._role，與 FB觀看 閘同機制）。
- **前端**：訂單詳情 modal 加 🗑「刪除訂單」鈕（`isAdmin` 限定，紅框樣式）→ `deleteOrderFromDetail(orderNo)`：`confirm()` 二次確認 → `api deleteOrder` → toast + 關 modal + `loadOrderMgmt()`。
- **無復原**：刪除即 deleteRow，不可還原（有記異動紀錄留痕）。

### 37.3 驗收 & 部署
- 沙盒（新建臨時 test 部署 @13，驗完已 undeploy）：admin 建單→倉管刪(擋)→admin 刪(成功)→getOrders 確認消失，前端 🗑 鈕 admin 限定+點擊實刪，全 PASS。
- 空酒款警示沙盒：空酒款→紅框顯示+捲動；輸入酒款→自動隱藏；正常單照存。全 PASS。
- 正式：後端 deploy-gas Action、前端 `promote.sh v2.5`。**正式端實測 deleteOrder**：用 admin 清掉 2 筆查證測試單（NPW-20260720-001/002），真單（NPW-20260704-002）保留無誤。
